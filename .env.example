# Postgres connection (docker-compose "db" service, or your own instance)
POSTGRES_USER=mailpilot
POSTGRES_PASSWORD=changeme
POSTGRES_DB=mailpilot
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# OpenAI classifier
OPENAI_API_KEY=sk-your-key-here

# Optional: LangSmith tracing
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=

# Testing-only escape hatch to reprocess already-saved emails. Keep false in production.
FORCE_REPROCESS=false
